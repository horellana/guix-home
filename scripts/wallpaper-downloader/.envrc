# Activa automáticamente el entorno Guix al entrar en la carpeta (direnv).
# Emacs lo recoge vía envrc/direnv.el. Recuerda: `direnv allow` la primera vez.
watch_file manifest.scm
eval "$(guix shell -m manifest.scm --search-paths)"
